# Candidate walkthrough — 3 minutes 40 seconds

Record your screen and your own voice. Use the [public app](https://interview-studio-sxvl.onrender.com) and [GitHub repository](https://github.com/hcthakur2004/trao-interview-studio). Before recording, sign in to your own test account, open a **real generated** kit and keep the repo/CI page in another tab. The Render Free instance sleeps after inactivity; wake it before recording. Do not show `.env`, the Render environment page, API keys, database URI, passwords or the private assignment PDF. Speak in your own words if the script feels unnatural; preserve the factual claims.

| Time | Show on screen | Say |
| --- | --- | --- |
| **0:00–0:25** | Public dashboard → **New prep kit**. Point to JD, company URL and days fields. Close the dialog without submitting another job. | “This is Interview Studio. It turns a job description, a company website and the time you have into an editable interview-preparation kit. I built it as a full-stack app: Next.js for the interface, Express for the API, MongoDB for durable data, and Gemini for structured generation.” |
| **0:25–0:55** | Open the real **PostHog Frontend Engineer** kit. Point to three requirements, complete coverage and the one-day plan. | “This kit was generated on the deployed service from a short frontend description. The extraction keeps an exact quote for each requirement, so the app can check that the requirement really came from the job description. Coverage is computed in code rather than assumed from the model’s answer.” |
| **0:55–1:20** | **Research & sources**. Scroll through actual retrieved page URLs and hiring context. | “The research step follows links from the supplied company site, including relative and nested careers links. It enforces robots rules, DNS and redirect checks, size and time limits, and labels missing evidence honestly. Public interview discussion is searched separately and treated as anecdotal.” |
| **1:20–2:05** | **Question bank**. Show the edited, pinned TypeScript question. If you want to demonstrate live regeneration, click **Regenerate category** and cut only the waiting portion; then show the pinned question remains. | “Questions are generated by category against the extracted requirements. If a must-have lacks a question, the pipeline makes a targeted repair pass and rechecks coverage. Editing is a first-class feature: a question I change or pin is protected when I regenerate its category. Saves use revision checks, so a stale tab cannot silently overwrite newer work.” |
| **2:05–2:30** | **Flashcards**. Reveal an answer, rate confidence, then show the reviewed count. | “Flashcards come from the question and answer outlines. A practice session puts unseen and lower-confidence cards first. Ratings are saved separately from generated content, so I can keep practising after a reload.” |
| **2:30–3:05** | **Study plan**, then **Your readiness**. Point to exact requested day count and one requirement → evidence → linked questions/confidence. | “The schedule is deterministic and has exactly the requested number of days, including edge cases like one or sixty days. Readiness connects each JD requirement to its supporting evidence, questions and my confidence. Coverage shows whether a topic has a question; confidence shows whether I feel prepared.” |
| **3:05–3:40** | Open GitHub README and **Actions → Verify** (green run), then return to the public app. | “The batch CLI and web jobs use the same pipeline and validated output contract. My five live evaluation cases finished in 183.669 seconds, and the final suite has 59 passing tests plus a clean build and CI. A deliberate trade-off is one in-process worker per deployment: it keeps quota handling and job recovery simple for this take-home; horizontal scaling would require database leases and a shared rate budget. The crawler also cannot fully inspect login-only or JavaScript-only pages, so the app reports those gaps instead of inventing facts.” |

Keep the final cut between **3 and 4 minutes**. If a live model step takes longer, cut the wait and say that you cut waiting time; do not suggest it completed instantly. Do not claim that a live kit needed a repair pass unless its trace actually shows one. The deterministic test suite demonstrates a forced repair separately. The PostHog kit above completed in one pass.

For the submission form, provide the repository URL, the public app URL and your real video link. Verify that the video permissions allow Trao's reviewers to watch without requesting access. The candidate should explain one design decision and trade-off in their own voice.
